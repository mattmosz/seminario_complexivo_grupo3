# === Configuración del Dashboard Streamlit ===

# 🔹 MODO API (cuando el backend FastAPI esté activo)
# BASE_API_URL = "http://localhost:8000/v1"
# TOKEN = "coloca_tu_token_aqui_si_usas_auth"

# 🔹 MODO LOCAL (por defecto actual)
OFFLINE_CSV = "data/hotel_reviews_processed.csv"
